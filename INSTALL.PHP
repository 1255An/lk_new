<?php

define('allow_access_to_this_script', true);
require_once("CONFIG.PHP");


/////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// СОЗДАЁМ БАЗУ ДАННЫХ
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////
echo "<b>ИНИЦИАЛИЗАЦИЯ БАЗЫ ДАННЫХ</b></br><hr>";

foreach(array_keys($TABLES) as $table){ // ОБРАБОТКА МАССИВА
    echo "Удаляем таблицу <b>".$config_db_prefix."_".$table."</b>, если уже существует.</br>";
    db_drop($table); // УДАЛЯЕМ ТАБЛИЦУ
    echo "Создаём таблицу <b>".$config_db_prefix."_".$table."</b> со столбцами:</br>";

    foreach($TABLES[$table] as $key => $value) // ВЫВОДИМ СТОЛБЦЫ И ТИП ДАННЫХ
    {
        echo "&emsp;&emsp;&#8226; ".$key." &rarr; ".$value."</br>";
    }

    db_create($table,$TABLES[$table]); // СОЗДАЁМ ТАБЛИЦУ
    echo "Таблица <b>".$config_db_prefix."_".$table."</b> завершена.</br><hr></br></br>";
}

echo "<b>БАЗА ДАННЫХ ГОТОВА К РАБОТЕ.</br>ФАЙЛ СЛЕДУЕТ УДАЛИТЬ НА СЕРВЕРЕ.</b>";
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////


?>